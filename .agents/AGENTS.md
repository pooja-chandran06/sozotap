# SOZOTAP Agent Rules

## Git Push Rule
- Automatically add, commit, and push changes to the remote Git repository (`origin main`) after completing every phase or significant feature, without asking for explicit confirmation.
